<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #f5f6f7 0%, #ffffff 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
      color: #222;
    }

    .container {
      background: #ffffff;
      border-radius: 10px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.06);
      padding: 40px;
      max-width: 600px;
      width: 100%;
    }

    .success-header {
      display: flex;
      align-items: center;
      margin-bottom: 30px;
    }

    .checkmark {
      width: 50px;
      height: 50px;
      background: #666;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 30px;
      margin-right: 15px;
    }

    h1 {
      color: #222;
      font-size: 28px;
    }

    .success-message {
      background-color: #fafafa;
      border-left: 4px solid #999;
      color: #444;
      padding: 15px;
      border-radius: 5px;
      margin-bottom: 30px;
      font-size: 14px;
      font-weight: 500;
    }

    .data-section {
      background-color: #f5f5f5;
      padding: 25px;
      border-radius: 8px;
      margin-bottom: 30px;
    }

    .data-section h2 {
      color: #222;
      font-size: 18px;
      margin-bottom: 20px;
      border-bottom: 2px solid #ddd;
      padding-bottom: 10px;
    }

    .data-item {
      display: flex;
      margin-bottom: 15px;
      padding: 10px;
      background: white;
      border-radius: 5px;
    }

    .data-label {
      font-weight: 600;
      color: #444;
      min-width: 100px;
      font-size: 14px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .data-value {
      color: #555;
      margin-left: 20px;
      word-break: break-word;
      flex: 1;
    }

    .button-group {
      display: flex;
      gap: 10px;
    }

    button, a {
      flex: 1;
      padding: 12px 24px;
      border: none;
      border-radius: 5px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      text-align: center;
      text-decoration: none;
      transition: transform 0.2s, box-shadow 0.2s;
      text-transform: uppercase;
      letter-spacing: 1px;
      display: inline-block;
    }

    .btn-primary {
      background: linear-gradient(135deg, #5a5a5a 0%, #3d3d3d 100%);
      color: white;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 20px rgba(60, 60, 60, 0.2);
    }

    .btn-secondary {
      background: #e6e6e6;
      color: #333;
    }

    .btn-secondary:hover {
      background: #dcdcdc;
      transform: translateY(-2px);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="success-header">
      <div class="checkmark">✓</div>
      <h1><%= title %></h1>
    </div>
    
    <div class="success-message">
      Thank you for submitting the form! Your information has been received and processed.
    </div>
    
    <div class="data-section">
      <h2>Submitted Information</h2>
      
      <div class="data-item">
        <span class="data-label">Name:</span>
        <span class="data-value"><%= userData.name %></span>
      </div>
      
      <div class="data-item">
        <span class="data-label">Email:</span>
        <span class="data-value"><%= userData.email %></span>
      </div>
      
      <div class="data-item">
        <span class="data-label">Message:</span>
        <span class="data-value"><%= userData.message %></span>
      </div>
    </div>
    
    <div class="button-group">
      <a href="/" class="btn-secondary">← Back to Form</a>
      <button type="button" class="btn-primary" onclick="alert('In a real application, this would save to a database!')">Save Data</button>
    </div>
  </div>
</body>
</html>
